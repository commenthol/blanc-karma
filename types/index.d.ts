/**
 * @param {Uint8Array} uint8Array
 * @returns {string}
 */
export function bytesToBase64(uint8Array: Uint8Array): string;
/**
 * @param {string} base64
 * @returns {Uint8Array}
 */
export function base64ToBytes(base64: string): Uint8Array;
/**
 * @param {number} length
 * @returns {string}
 */
export function getRandomString(length: number): string;
